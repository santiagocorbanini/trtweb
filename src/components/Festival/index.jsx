const Festival = () => {
    return (
        <section
            className="page-section d-flex align-items-center justify-content-center"
            id="inicio"
        >
        </section>
    )
}

export default Festival
